<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Hitung durasi pemakaian per aplikasi (UsageStatsManager) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground service untuk monitoring pemakaian berjalan terus -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Notifikasi wajib (Android 13+) untuk foreground service & live screen -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Overlay untuk layar lock saat waktu habis -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Auto-start service setelah reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Live screen monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <application
        android:name=".ChildApplication"
        android:allowBackup="false"
        android:label="Waktu Sehat"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.ChildApp"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ChildApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Layar kunci full-screen saat batas waktu habis / dikunci parent -->
        <activity
            android:name=".ui.LockScreenActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.ChildApp.Lock" />

        <!-- Minta izin izin sistem (Usage Access, Overlay, Accessibility, Device Admin) -->
        <activity
            android:name=".ui.PermissionSetupActivity"
            android:exported="false" />

        <!-- Trigger dialog izin MediaProjection dari sistem (transparan, langsung selesai) -->
        <activity
            android:name=".ui.ScreenCapturePermissionActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Foreground service: pantau pemakaian & enforce limit -->
        <service
            android:name=".service.UsageMonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Foreground service: live screen capture (MediaProjection) -->
        <service
            android:name=".service.ScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- AccessibilityService: deteksi app aktif & force-close app yang diblokir -->
        <service
            android:name=".service.AppBlockAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Device Admin: mencegah uninstall/disable tanpa PIN parent -->
        <receiver
            android:name=".receiver.ChildDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Auto-start service setelah reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
